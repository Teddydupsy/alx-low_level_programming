#include "main.h"
/**
 * _islower - Code to check lowercase letter
 * Letter c is the case-study
 * Return: 1 if c is lowercase, else 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	else
	{
		return (0);
	}
	}
}
