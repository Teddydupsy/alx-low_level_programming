#include <stdio>
#include "main.h"
/**
 * main - prints the name of program
 * @argc: arguments count
 * @argv: array of arguments
 * Return: 0 (success)
 */
int main(int argc_attrubute_((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
