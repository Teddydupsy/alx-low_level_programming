#include <stdio.h>
/**
 * main - lowercase letters
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha;
	for (alpha = 'a'; alpha <= 'z' ; alpha++)
	{
		putchar(aphabet);
	}
	putchar('\n');
	return (0);
}
